<div class="navbar admin">
  <span class="navbar-title"><%= t('.admin_panel') %></span>

  <div class="navbar-admin user">
    <%= t('.welcome') %>, <b class="navbar-admin user-email"><%= current_user.email %></b> <%= t('.guru') %> |
    <%= link_to t('.logout'), destroy_user_session_path, method: :delete, class: 'navbar-link' %>
  </div>
</div>
