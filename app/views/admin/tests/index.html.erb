<h2><%= t('.header') %></h2>

<p>
  <%= link_to t('.new'), new_admin_test_path, class: 'btn btn-primary mb-3' %>
</p>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= t('.title') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.category_id') %></th>
      <th><%= t('.questions') %></th>
      <th colspan="3"><%= t('.actions') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @tests %>
  </tbody>
</table>
