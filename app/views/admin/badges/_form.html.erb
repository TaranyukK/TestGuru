<%= form_with model: [:admin, @badge], local: true, class: 'form-horizontal' do |f| %>
  <%= render 'shared/errors', resource: @badge %>

  <div class="mb-3">
    <%= f.label :image_url, class: 'form-label' %>
    <%= f.text_field :image_url, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= f.label :name, class: 'form-label' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= f.label :rule, class: 'form-label' %>
    <%= f.select :rule, BadgeService.badge_rules.map{|rule| [t(".#{rule}"), rule]}, id: :badge_rule, class: 'form-select' %>
  </div>

  <div class="mb-3">
    <%= f.label :option, class: 'form-label' %>
    <%= f.text_field :option, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
