<nav class="navbar">
  <div class="navbar-section navbar-left">
    <%= link_to t('testguru'), root_path, class: 'navbar-logo' %>
    <%= link_to t('.all_tests'), tests_path, class: 'navbar-link' %>
  </div>
  <div class="navbar-section navbar-right">
    <% if user_signed_in? %>
      <span class="navbar-text"><%= t('.welcome', full_name: current_user.full_name) %></span>
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete, class: 'navbar-link' %>
    <% else %>
      <%= link_to t('.sign_up'), new_user_registration_path, class: 'navbar-link' %>
      <%= link_to t('.log_in'), new_user_session_path, class: 'navbar-link' %>
    <% end %>
  </div>
</nav>
